#include <bits/stdc++.h>
using namespace std;

int main() {
    freopen("/home/leksus/Data/Competitive-Programming/IO/input.txt", "r", stdin);
    
    int TC;
    cin >> TC;

    while (TC--) {
        int n, q;
        cin >> n >> q;
        vector<int> a(n);
        for (auto&& ai : a) cin >> ai;

        
    }
}